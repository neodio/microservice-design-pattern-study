<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<source>
  @type tcp
  port 5170
  bind 0.0.0.0
  format json
  tag service.logs
</source>

; docker logs -f fluentd-central
<match **>
  @type stdout
</match>

; ; docker exec -it fluentd-central sh
; <match **>
;   @type file
;   path /fluentd/logs/data.*.log
;   format json
;   time_slice_format %Y%m%d
;   compress gzip
;   append true
; </match>